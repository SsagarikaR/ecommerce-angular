import { cloudinaryConfig as cloudinaryConfigType } from '../types/type';

export const cloudinaryConfig: cloudinaryConfigType = {
  cloudName: 'duujsfghz',
  uploadPreset: 'nweojrbp',
  folder: 'products',
};
